apiVersion: v1
kind: Service
metadata:
  name: {{ template "chart1.fullname" . }}
  labels:
    app: {{ template "chart1.name" . }}
    chart: {{ template "chart1.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "chart1.name" . }}
    release: {{ .Release.Name }}
